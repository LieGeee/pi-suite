Agent workflow and verification: On interruption or "continue," resume the current explicit task before adjacent work. For an explicitly read-only review, make no edits: report comprehensive, prioritized findings with concise remedies first, then edit only after approval. If subagents are used, the main agent continues its independent work and verifies child claims against actual target files plus session/tool evidence; a child may complete filesystem writes even when the parent extension reports an error. In Pi's custom subagent extension, create `stdoutLines` before awaiting the child process because post-exit JSONL diagnostics use it; one subagent call awaits its child, and only independent sibling calls emitted together provide true parent-side parallelism. <!-- created=2026-08-03, last=2026-08-04 -->
§
`xl-ht` validation: CodeGraph fails on multiword arguments, so use `rg`, symbols, or direct reads. Action-route smoke tests need healthy ports, signed/HMAC responses, and database state; do not infer behavior from transient Nacos metadata. For live MySQL, keep the Nacos password only in temporary `MYSQL_PWD`, inspect `information_schema` before SQL, and do not assume legacy `sys_base_wt` has `del_flag` or `order_no`. Vue2/Element UI: expose module constants via method/computed, keep tables mounted, set `empty-text`, and count rows only when `height > 0`. Contracts: `S:/note/xl-ht/商务委托单/08-客户订单子委托动态表单与任务闭环/`. <!-- created=2026-08-03, last=2026-08-04 -->
§
[tool-quirk] CAPTCHA-gated site checks: assess public information, domain, certificate, and reputation first. If login is needed, start isolated Edge through Playwright/CDP (unique debug port and temporary profile), let the user complete CAPTCHA, then take read-only control while intercepting external domains and downloads. Close it and delete the temporary profile/cookies; never retain credentials. <!-- created=2026-08-04, last=2026-08-04 -->
§
IDEA 运行 `HhSystemApplication` 是热部署外的独立 JVM，新增 Controller/路由/Feign 接口必须重启才生效。重启后旧 token 会过期，需浏览器重新登录。判断服务是否加载最新代码：查 8882/8084 端口进程的 PID 和 StartTime，新进程应晚于最新 git commit。Nacos 实例无 commit 元数据（IDEA 直接启动不会注入），但可通过 HTTP actuator health 确认服务 UP。 <!-- created=2026-08-04, last=2026-08-04 -->
§
Pi subagent latency investigation Aug 2026: direct provider `5.6和自动` repeatedly produced 503/404 and minute-scale timeouts (historical firstToken up to 133s); `随时qh` local aihub-auto at 127.0.0.1:8787 was healthy (models endpoint 200) and live child smoke measured scout ~3.9s total, luna ~8.6s. Default scout/planner/worker/reviewer/reviewer-sol agent frontmatter was switched from 5.6和自动 to 随时qh; model/thinking settings preserved. <!-- created=2026-08-04, last=2026-08-04 -->
§
https://www.azureflame.cloud/quiz-drowning.html 是伪装成官方"暑期防溺水安全知识调查问卷"的恐怖整蛊页（伪造政府备案样式、突脸惊吓图、低频音效、推广 QQ 群），不会提交真实数据；用户再要求填写此类问卷时不要当真、不要填写个人信息。 <!-- created=2026-08-04, last=2026-08-04 -->
§
用户要求所有 dpsk subagent 的思考强度使用最大档：agent frontmatter 写 `thinking: xhigh`，因为当前 dpsk/deepseek-v4-pro 与 deepseek-v4-flash 的 thinkingLevelMap 都是 `xhigh: max`。不要在 frontmatter 直接写 `max`。 <!-- created=2026-08-04, last=2026-08-04 -->
§
Pi extension tool calls are turn-blocking: executionMode="parallel" only runs sibling child tasks inside one tool call, and the parent model cannot continue unless it emitted its own sibling tool calls in the same assistant response. The custom S:/tool/pi subagent extension now supports explicit background:true with parallel tasks: it returns a job id immediately, continues children under an independent AbortController, and posts results via a followUp custom message; background work requires explicitUserRequest=true and non-overlapping paths. <!-- created=2026-08-04, last=2026-08-04 -->