AI 工厂 IAM、执行与系统边界：Casdoor 仅负责 AuthN/IdP（登录、MFA、OIDC/OAuth、Token），Portal BFF/Business Gateway 用 JWKS 本地验签；自建 Go IAM/AuthZ 管理用户映射、组织/Tenant、成员、角色、权限、数据范围和审计。工厂/项目/仓库是授权范围；Gateway 认证，业务服务对写、审批、敏感读/导出执行 `RequirePerm()`，Repository/ORM 从可信 Context 注入 fail-closed Data Scope；资源归属、状态、幂等仍由业务系统校验。RunCall 账本承担调用幂等、Capability 单次消费和执行状态，durable Audit Outbox 先落意图再执行并异步投递权威审计库，支持审批、恢复和对账。WMS 等外部系统仅通过稳定 OpenAPI/MCP 契约集成，不直连数据库、不复制其业务规则。 <!-- created=2026-07-27, last=2026-07-30 -->
§
`S:/note/zt` AI 工厂文档约定：保持少量、克制的主文档和父子节点；根总览只列父节点，子文档放同名目录并与父节点双向链接，面向基础读者，避免把入门、正式设计和源码审阅平铺成大量文章。现有父节点：`02-Go IAM权限模型与接口.md` + `02-Go IAM权限/`，`03-AI控制面与执行平面.md` + 同名子目录。权限图中 Casdoor 固定为身份与权限层独立服务，登录/刷新/退出用虚线，业务请求用实线。改 Vault 前从 `/s/note` 做 tar 备份并独立验证；若 Obsidian 锁目录导致 swap 失败且确认未部分移动，则文件级覆盖并逐文件 `cmp`。 <!-- created=2026-07-27, last=2026-07-30 -->
§
AI 工厂控制面、Runtime 与模型渠道：Go AI Control Plane 管 Agent Profile、Runtime、Skill/MCP、模型渠道、配额与治理；LangGraph 为长期主 Runtime，Dify 为内部低代码 Runtime，均由 Profile 选择并经统一 Run API。生产仅用官方 API/企业云，开发测试可用隔离 Sub2API，禁止跨环境和信任边界。AI Gateway 自动路由采用 A 方案：借鉴 aihub-auto v0.2.3（commit 316272333bc3a859a8aea6e1750019a77b77c564）的会话亲和、P2C、Peak-EWMA、熔断；仅在同一 Model Alias 等价 Endpoint Pool 内切换，不跨模型。安全失败可在 Stream Commit Barrier 前重试；TTFB 超时、发送后断连和模糊 5xx 默认 unknown，不透明重放。用户确认已获作者复制许可；生产前归档书面授权、归属和 SBOM/NOTICE。Pi CLI/RPC 为 Phase 2A Coding Harness，企业入口 corp-ai/IDE，关闭个人自动发现，仅消费签名 ResolvedAgentProfile。 <!-- created=2026-07-29, last=2026-07-30 -->
§
企业永久记忆应是独立 Memory Service，按 tenant/user/project/classification 隔离，并支持授权、审计、保留期和可验证删除。Pi Hermes Memory 仅作单用户本地机制参考，可借鉴 policy-only、内容扫描、纠错即时保存、会话 FTS、压缩前 flush、Memory/Skill 分离，不能直接承担企业多租户记忆。 <!-- created=2026-07-29, last=2026-07-30 -->
§
AI 工厂代码知识与沙箱：采用“项目 ACL 过滤后的检索定位 + 受控 checkout 按 `repo@commit + path + lines` 精读并生成 Citation”。Go Project Access Resolver/Source Gateway 负责 ACL、`project_id -> repo@commit`、多项目 Registry、commit/index 绑定和审计，客户端不得提交任意路径。Zoekt（commit 2cb19912a407，Apache-2.0，无正式 release）承担多仓 lexical retrieval，结果含 RepositoryID、索引 Commit、分支、行号、内容/checksum，并支持 RepoIDs 过滤；standalone 无 AuthN/AuthZ，必须藏在 Resolver 后。CodeGraph 0.9.4（MIT）互补承担单仓 AST、符号和调用图，但原生 `projectPath` 无企业 ACL且索引不绑定 Git Commit。SCIP v0.9.0 是协议/索引格式而非查询服务，Metadata 无 Git commit，当前延后。OpenHuman e213bc5（GPL-3.0）仅参考 SuperContext、层级摘要、worktree/Docker Sandbox，其 GitHub Source 不索引源码正文、Fleet 非生产多租户、Windows AppContainer 会降级 Noop。Daytona 公开核心已于 2026-06 停止维护，不采用；E2B 自托管完整但较重；Microsandbox（Apache-2.0）轻量但仍为 beta，缺企业调度、IAM、审计和多租户控制面。 <!-- created=2026-07-30, last=2026-07-30 -->
§
AI 工厂 MCP 治理（源码审阅 2026-07-30）：ToolHive v0.41.0 / 3d44435d7bfc（Apache-2.0）支持 OIDC、携带 tool args 的外部 HTTP PDP、未知方法默认拒绝、授权工具过滤、vMCP、Redis 限流和 OTel，可作 MCP 数据面薄封装；但限流后端错误 fail-open，且无高风险调用审批/恢复闭环，因此 Go IAM、RunCall、审批和权威审计必须外置。ContextForge v1.0.6 / 75e9ccc01de7（Apache-2.0）内建 Team/RBAC/Admin UI/DB/A2A，与现有 Go IAM 重叠；其 `REQUIRES_APPROVAL`/`requires_review` 只是标记而非审批工作流，仅作参考。 <!-- created=2026-07-30, last=2026-07-30 -->
§
AI 工厂权限审阅修正：Data Scope 只回答“允许访问哪些数据”，不能替代功能权限；普通业务读取也必须声明基础 Read Permission（如 `wms.stock.read`），写、审批、导出和敏感读取继续声明对应 Permission，Repository/ORM 再叠加 fail-closed Data Scope。`platform_user` 状态机须包含 `provisioning`（并定义失败、重试、激活和禁用语义），与 Casdoor 账号 Outbox 开通流程一致。 <!-- created=2026-07-31, last=2026-07-31 -->
§
AI 工厂执行与证据链修正：人工审批通过后必须重新签发绑定 `approval_ref + action_digest` 的短时单次 Capability，不能复用审批前凭证。各服务在自身持久化边界内原子写业务状态、`audit_event` 和 durable outbox；多生产者不能共同维护天然全序的 Run 级 `prev_event_hash`，应按生产者/资源流分链，再由带序号的聚合清单汇总。外部结果在 terminal event 持久化前不得向 Runtime/用户提交成功。Knowledge Service 仅作为上传文件、网页等企业文档权威源；Git 源码及仓库文档的权威源固定为 `repo@commit`，经 Project Access Resolver/Source Gateway 授权复读并生成包含路径、行号和内容 Hash 的 Citation，删除后仅保留合规 Tombstone/最小证明。 <!-- created=2026-07-31, last=2026-07-31 -->
§
统一 IAM 的产品与命名边界：对员工和管理员应呈现一个统一 IAM 门户；内部实现仍拆为 Casdoor AuthN/IdP 与 Go Corp AuthZ。Casdoor负责密码、MFA、OIDC、Token 和 IdP Session；Corp AuthZ 负责企业主体映射、公司成员、角色、Permission、Data Scope 和撤权。文档与界面优先使用“Corp AuthZ/企业授权中心”，避免把 Go IAM 误解为第二套登录系统。 <!-- created=2026-07-31, last=2026-07-31 -->