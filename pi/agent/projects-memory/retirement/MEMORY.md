retirement 后端与数据契约：平台管理员仅 `RETIREMENT_PLATFORM_ADMIN_*`，Demo 仅 `RETIREMENT_SEED_DEMO=true`；密码/JWT/gRPC 密钥至少 32 字符。登录仅手机号或平台账号+密码；JWT 必验 issuer、expiration、scope、must_change_password，租户/人员只信 JWT，平台 token 禁止访问租户 API。账号、角色、组织、人员关系和业务引用必须同租户；可登录人员需 phone、role_id、person:create、role:assign 且至少一角色。组织 COMPANY→DEPARTMENT→TEAM（兼容根 DEPARTMENT），有效关联不可删，人员组织关系软删。gRPC 仅 127.0.0.1:9001-9006 且校验共享密钥；memory/PostgreSQL 语义须一致并拒绝跨租户。审计不记录正文/凭据/附件，失败不回滚业务，查询需 audit:view。空间层级递增且 BED 无子；入住床不可删/改类型，停用或不可用床不得入住；入住退住仅 CheckInElder/CheckOutElder，列表与床位看板同源并加租户锁/唯一索引防并发。调度启用校验模板和六字段 Cron；循环仅年/月/周/日；幂等键调度ID+触发秒+老人/护理员，日常护理为护理员+模板+老人+日期。ASR 用 DashScope qwen3-asr-flash Chat Completions，音频 Base64 Data URL，API Key 仅 Gateway 环境；已暴露 sk-ws Key 正式前必须轮换。老人批量新增须后端 POST /elders/batch（事务、一次权限校验、逐行失败索引/原因），支持多行表单与 Excel，可选紧急联系人。替班已云端验证（迁移 000021）：语音建申请，task:assign 管理员审批，批准须指定接班护理员，仅转移班次窗口内待执行任务，执行中保留原护理员，权限 shift:view/request/approve，申请/审批范围隔离。 <!-- created=2026-07-25, last=2026-08-03 -->
§
retirement 移动端业务/UI契约：底栏固定工作台/今日任务/消息/我的/机构；机构对全部租户角色可见，护理员/维修员只看入住、床位、通知等概览，管理能力按 permissions 开放，REPAIRMAN 保留专用工作台。管理首页仅任务分发、快速创建、语音创建；老人/床位、人员/组织、统计、知识库归“机构”，模板、循环调度、字典、语音策略归“机构 > 基础配置”。全业务无抢单/认领：未指定执行人进入待分发，分配后 PENDING_PROCESS（前端“待执行”），历史 PENDING_ACCEPT 仅兼容。护理语音按任务类型、老人及照护关系分配本人/责任护理员；维修待分发，老人不明必须补选。语音策略 AUTO 或指定具 task:assign 的 REVIEW 审核人，失效不得静默绕过；task_drafts 留存原文/识别/依据/执行人/终态且院长可见。任务派发、指定创建、转派、转维修、异常、自动超时产生幂等消息。账号级通知、默认班次、工作台排序、字体偏好服务端同步；图片质量、仅 Wi-Fi 上传、清缓存仅本机。复用 operations.mjs、manager-actions.mjs 与统一空/错/状态/统计；`GET /api/v1/dict` 是前端中文枚举唯一权威，Gateway 聚合显示名，UI 不暴露内部 #ID。知识库资料可编辑，问答限当前租户并显示引用，依据不足固定答“知识库中暂无依据”。护理员及低文化程度/老年用户默认简洁模式和大字号（标准/大/特大）；护理员首页突出已分发任务、开始/完成、护理记录、异常上报和 App 内语音悬浮球。护理员任务排序：有关联老人任务优先，按老人姓名/床位分组，组内按执行中、紧急程度、截止时间；无老人维修/清洁归“其他事项”置底。App-PLUS 语音悬浮窗必须用独立 plus.nativeObj.View 原生覆盖层，页面主体保持全宽；全屏业务弹窗打开时 hide，关闭/卸载后恢复。替班移动端页面 pages/shift/{index,form,detail} 已接消息中心和语音入口；App WebView 无 URLSearchParams，查询串手工拼。运营 UI 变更须验证 test:ui、H5/App 构建和 Android 角色流；UIAutomator null root 时等稳定后重抓。 <!-- created=2026-07-26, last=2026-08-03 -->
§
retirement 工作方式、环境、发布与部署：优先完善功能，未经请求不主动安全加固；禁止子代理/子线程，调查、修改、验证均在当前会话完成。文档根 `S:\note\yanglao`，测试账号唯一来源 `S:\note\yanglao\测试账户\00-总览.md`；客服/演示密码为人名拼音首字母大写+6位数字。后端 `S:\code\retirement\retirement`（Gateway :7000），前端 `S:\code\retirement\retirement-app`，未经许可不得重启/终止用户的 :7000。远程 PostgreSQL `retirement` 是预发布集成测试展示库，不要启动本地 Docker，仅可修改测试展示；迁移前只读预检，至少到 000017；无库设 `RETIREMENT_DB_MODE=memory`，真实库只设 `RETIREMENT_DB_DSN`，绝不记录/回显/写入 DSN 或凭据。公网 Gateway `x.x.x.x:7000`（Ubuntu 22.04，systemd `retirement-gateway.service`，二进制 `/opt/retirement/current/retirement-gateway`）；新增后端路由部署后验证公网非 404，并查 `/healthz`、`/ai/voice-task-policy`、`/rag/answer`。SSH 密钥不可用；仅用户当次主动提供 root 密码时可用，密码不得存储/回显，可 Paramiko/SFTP 替换二进制并重启（SSH 状态 2026-08-04：root 密码状态不确定，部署脚本 `.runtime/deploy/deploy_with_config_password.py` 从 configs/gateway.yaml 推导候选取，Linux ELF 在 `.runtime/deploy/retirement-gateway` SHA-256:6167cc0c…，云端库已公网 9887 应用至 000022；下次部署前需用户在阿里云确认密码或加 SSH 公钥）。Android 公网 API `http://x.x.x.x:7000/api/v1`，本地 H5 `127.0.0.1:7000`，模拟器 `10.0.2.2:7000`。Android/App 构建用 HBuilderX 5.14 与 `S:\tool\anzhuosdk`，模拟器 API30 x86 `pi_mobile_api30_x86`，DCloud 依赖 3.0.0-5010420260703001；`uni build -p app` 只生成资源不是 APK，App 产物须含 `__uniappview.html`、app-service.js、app-config.js、manifest.json、app.css，H5 成功不能替代 App 验证。正式包须独立 DCloud AppID、`com.retirement.mobile`、正式签名、支持 plus.* 的离线壳；测试 APK 可嵌入 HBuilderX 标准基座（AppID=HBuilder，包名 io.dcloud.HBuilder，仅测试），桌面名/图标"养老助手"，交付 `G:\cunchu\养老助手-本地实机测试.apk`。并行验证 Android 勿用 emulator-5562（或被 HBuilderX/其他流程控制反复切换前台），改独立端口、独立 API30 x86 AVD 及 G: 单独 writable userdata，勿操作/停止 5562。数据库备份、部署备份清单及相关本地制品不得放 C:；本地 DB 备份统一存 `S:/code/retirement/retirement/.runtime/backups/database/` 且不入 Git。乱码审计：`attachment-e2e-*`、`care-manager-e2e` 等 ASCII E2E 是联调测试数据；仅连续问号、Unicode `�` 或明确误解码才算编码损坏。 <!-- created=2026-07-26, last=2026-08-04 -->
§
retirement 迁移与隔离补充（2026-08）：云端库已到 000022。000018 通知文案统一“待执行”；000019 精确修复 0727 联调乱码（连续 ???? 与 �，只按精确 ID+旧值）；000020 拆出独立 audit:view（仅 ADMIN/DIRECTOR，task:assign 不再等于审计权）；000021 替班（shift:view/request/approve）；000022 定期任务批量分配：task_schedule_assignees 表 + assignee_mode LEGACY/EXPLICIT，原绑定人员取并集默认勾选、原子替换、50 计划×50 人/1000 任务上限、护理任务按所选护理员负责老人拆分、无老人跳过、幂等防重放、LEGACY 计划行为不变。角色数据范围：护理员仅本人负责老人返回完整档案，其余老人只姓名+床位，详情/家属/照护接口 403；经理可见组织子树+直属下属（DataScopeResolver，创建自动绑定主组织）。 <!-- created=2026-08-03, last=2026-08-03 -->
§
retirement 护理员悬浮语音球在最新 APK 已验证正常：CAREGIVER 角色权限含 task:report + shift:request，登录后 plus.nativeObj 悬浮球在首页/今日任务页均存在并渲染（exists:true + 像素分析确认右下蓝色圆形）。用户"护理员手机没小球"的根因是手机装的旧 APK 不含悬浮球功能（floating-voice.mjs 为未提交新文件），需更新 G:\cunchu\养老助手-本地实机测试.apk。验证用护理员 13800000003 密码临时改为 admin123（原哈希 $2b$12$Z64xNJdw/I1DLe8Z0gphmOLokZTIwFpek4NY.QSarRbusKNH4ukL6 已备份）。 <!-- created=2026-08-04, last=2026-08-04 -->