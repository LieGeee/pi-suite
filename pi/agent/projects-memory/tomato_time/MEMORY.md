tomato_time core contracts: Use one hierarchical, multi-facet Tag model across the Home word cloud, card pools/draws, tasks, and plans; do not introduce string-label models. GORM models use `ID`/timestamps; web normalizes tags and selectors emit numeric IDs. Cards use `Card`/`DrawHistory` with legacy `cards_json` fallback and authorized feedback. Plan-task preview/apply/unlink rolls up progress and blocks started/completed conflicts. Pomodoro responses use camelCase; clients must tolerate legacy snake_case. Natural expiry completes/counts, manual stop cancels, paused time is excluded, starts are per-user locked, and completion is conditional/atomic. Daily tasks use `/api/v1/tasks/daily/*`, Asia/Shanghai dates, unique `(user_id,date,task_id)`, and idempotent add/carry-forward/sort shared by web and desktop. Concurrent SQLite tests require a temp-file DB plus `busy_timeout`, not shared in-memory SQLite. <!-- created=2026-08-03, last=2026-08-04 -->
§
tomato_time engineering/deploy: `server/` (Go) and `web/` (Vue) are separate modules in `go.work`; never run `go test ./...` at repo root. Use `npm run ci` for server/web tests, type checks, zero-count route/hygiene checks, build, and `git diff --check`. Secrets come from `config.json` or `TOMATO_*`; GitHub uses `git@github.com:LieGeee/tomato_time.git`. The user manually deploys production to `root@x.x.x.x`: binary `/www/wwwroot/tomato_time/server/tomato_server`, web `/www/wwwroot/tomato_time/web/dist`, and Nginx `:8979` proxies `/api/` to `127.0.0.1:8080`. Use `nohup` and mode-600 `server/tomato_time.env`, preserving `TOMATO_SERVER_PORT=8080` and `TOMATO_JWT_SECRET`. This machine has no deployment SSH. Public API: `http://x.x.x.x:8979/api/v1`; test credentials: see project docs. <!-- created=2026-05-23, last=2026-08-04 -->
§
tomato_time Windows desktop: `desktop/` is Tauri 2 + Vue 3 with login -> 76x76 widget -> 340x520 panel, tray actions, single-instance behavior, persisted clamped widget position, Credential Manager auth, 15s/focus resync, task lock through complete/cancel, configurable dismissal, and carry-forward prompt. It uses `http://x.x.x.x:8979/api/v1`; `contract.ts` intentionally normalizes camelCase and legacy snake_case Pomodoro responses. `keyring` v3 requires `features=["windows-native"]` or tokens are lost after restart. Treat 401 as logout/login and only network failures as offline, because offline blocks writes. Omit `plugins.autostart` from `tauri.conf.json` (panic exit 101); use `LogicalSize`, grant `core:window:allow-start-dragging`, start dragging only after a pointer threshold, and resize the native window after auth changes. Build from VS DevCmd with Rust/VS2022 state under `D:\TomatoBuild\`; follow `build-tomato-time-windows-desktop`. Installer: `dist/番茄时间悬浮窗_0.1.0_x64-setup.exe`; installed app: `G:\tools\番茄时间悬浮窗\`. NSIS can retain a stale EXE, so verify mtime and size. Verify Vitest/build plus the installed shortcut at 100%/150% DPI, click-to-panel, drag, and single-instance. <!-- created=2026-08-03, last=2026-08-04 -->
§
tomato_time web/mini/desktop 三端共享每日任务 API；Web 首页通过 TodayTaskList 展示今日清单，支持完成、拖动排序、搜索加入、快速新建和顺延。Pomodoro 响应在 web/mini 端也必须经过 contract 归一化，兼容 camelCase 与旧部署 snake_case 字段。Web 的 VoiceInput 麦克风权限应在用户点击录音时懒加载，不能在首页挂载时自动申请。 <!-- created=2026-08-04, last=2026-08-04 -->
§
tomato_time Web UI layout convention: Home keeps the focus timer and pending tasks in the first row, with TodayTaskList as a full-width section below; mobile order is timer -> pending tasks -> today list. The unified task/plan workspace uses two readable main columns with integration context below; PlanWorkspaceContent collapses its internal list/detail grid via container query when embedded narrowly. Avoid three-column nesting that forces Chinese headings into one-character lines. TasksWorkspaceContent 同样按容器宽度响应；容器标记必须放在祖先工作台面板根节点（CSS 容器不能查询自身），grid 子项需 min-width:0，否则内部 overflow-x:auto 表格不生效（/tags 移动端 1056px 横向溢出即此原因，已修）。桌面半宽面板内计划摘要仍保持双列。AppHeader「更多」为真实下拉菜单（Persona/标签/分类/分心/专注记录/使用指南）；请求层错误短时间去重、500 统一提示「服务暂时不可用」。 <!-- created=2026-08-04, last=2026-08-04 -->
§
tomato_time 测试约定：「今日」类统计测试禁用 now.Add(-2h) 这类可能跨午夜的相对时间（00:00–02:00 运行会误判为昨天导致 flaky），一律用当天零点起算；TestStatsGetOverviewReturnsUserScopedStats 已按此修复。 <!-- created=2026-08-04, last=2026-08-04 -->