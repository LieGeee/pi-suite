xl--gt/go-xl login integration finding: xl--gt uses RuoYi-style Authorization: Bearer JWT containing user_key/user_id/username plus Redis session key login_tokens:{uuid}, frontend stores Admin-Token cookie; go-xl uses stateless HS256 access/refresh JWT with user_id/tenant_id/user_name and frontend localStorage access_token/refresh_token. Tokens are not directly interchangeable; preferred SSO integration is one-time ticket/token-exchange where the receiving system issues its own native token. <!-- created=2026-05-22, last=2026-05-22 -->