xl-ht 环境与长期协作约定：正式仓库 `S:/code/xl-ht`（通常运行/开发分支 `dev_business`）；长期资料在 `S:/note/xl-ht`，架构/实施方案也写仓库，方案阶段优先更新 `07-开发计划.md`、不改代码；`S:/note/zt` 属于其他系统，仅参考结构。Git Bash 用 `python`（`python3` 是 WindowsApps 占位），`psql` 不在 PATH；中文 MySQL DDL 加 `--default-character-set=utf8mb4`。HT MySQL 为 `182.92.72.159:33066`（`ft_base/ft_config/ft_data`），Go DB 端口 `35432`，Redis 为 `localhost:6379` DB3；不记录密码，tenant 缺失时用 `hh-system RemoteDeptService.getDeptById` 查 `sys_dept.tenant_id`。HT↔Go tenant 映射由系统管理员维护且严格一对一，普通用户不可切换、数据库连接不变；测试 HT 映射 Go tenant 86，正式上线切换到同一 Go/PostgreSQL 中已有正式 tenant，不新建数据库/Go tenant，也不迁移测试审批、任务、流程历史。worktree 提交须先合入实际运行分支再构建/重启并保留主工作区改动；`hh-ft-ui/node_modules` 用真实 junction，默认 UI `http://127.0.0.1:2999`。影子 JAR 启动前核对 `bootstrap.yml`；缺失时显式传应用名、Nacos 地址和 zihao namespace `6a96adcb-3bc6-4896-b6c6-4a882665b6ac`，Windows JVM 加 `-Dfile.encoding=UTF-8`。JUnit 5 用 Surefire >=2.22.2；`hh-file` 缺类先执行 `mvn -pl hh-modules/hh-file -am install`。仓库忽略 `docs/`，`docs/superpowers` 文档需 `git add -f`。 <!-- created=2026-07-22, last=2026-08-04 -->
§
xl-ht CY01 堆场约束（tenant 9）：以 `ft_stock_yard_slot` 为布局事实源，倍位 1-12、最高 3 层；639 个 PHYSICAL 可放柜，30 个 BLOCK 只占 Y footprint；网格外区域用 `ft_stock_yard_layout_zone`，未确认坐标不得入库。状态 `0队列→1已分配→2入库中→3待出库→4已出库`；取消从 3 回 2 且保留位置，历史只追加。20* 只排奇数 Y 单格，40*/45* 只排偶数 Y 双格，未知箱型禁排；`admission_id` 可空，非空必须关联真实单据；时间格式 `yyyy-MM-dd HH:mm:ss`。工作台左队列右矩阵；权威文档 `S:/note/xl-ht/仓储/堆场设计`。 <!-- created=2026-07-23, last=2026-08-04 -->
§
xl-ht 远端 `origin/dev_fleet_publish` 是独立车队端（车队登录、订单接收/拒绝、司机车辆绑定、异常反馈、待办、统计），不是司机本人 App；讨论、权限和实现必须严格区分。 <!-- created=2026-07-29, last=2026-08-04 -->
§
xl-ht 统一业务工作台与动作平台约定（2026-08）：一期聚合主键必须是稳定 Case/Intent/Draft 业务事项，不是业务表 ID、参与人或 Go task；一次入场/送货可挂申请、预约、作业子任务，周期巡检先生成实例。独立 Java `hh-workbench-bff` 以 `WorkItemDTO`/Provider SPI 实时联邦 `hh-application`、`hh-safety` 等权威服务，只负责当前用户范围查询、规范化、合并排序、局部降级和动作路由；不接 Go `task-service`，不把审批节点转业务任务，不跨库查询或编排跨服务写链，一期不建 `wb_work_item`。无稳定业务记录的待填写申请只能作为快速办理入口，审批仍由独立 approval 处理；动作级权限快照暂不补 `sys_menu`，按界面路由与角色控制。AUTO runner 已实现，路由 key 固定为 `WORK_ORDER_CREATE:{domainCode}`；定时调度、ASYNC 回调、独立动作平台、生产审批和高可用验收仍未接入。运输/仓储/安环/报关/结算 DTO/API 尚未提供，`domainPanelRegistry` 以 `available:false` 占位，载荷工厂注册表和事件 inbox 已就绪。P0 联调用 tenant 10 的 `PI/P0LIVE` 标记数据加签名直发，验证后清理 `ft_base/ft_data`。关键不变量：WORK_ORDER_CREATE `commandId` 用 `tenantId+entrustmentId+triggerStatus+actionType` 稳定键，不能用插入前的 `action.id`；事件按 `(sourceSystem,eventId)` 幂等并校验 `commandId/attemptVersion`，防止迟到或乱序事件回退终态；根单自动完成须满足非终态、至少一个有效子单、所有子单均为 `COMPLETED/SETTLED` 且无未耗尽创建动作。 <!-- created=2026-08-03, last=2026-08-04 -->
§
xl-ht 客户订单/商务委托最终契约（2026-08）：`sys_base_wt` 是客户订单唯一事实源；新订单 `bus_type=NULL`，不写 `sys_base_wt_hd/cc/ys`。旧 `sys_*` 表常无 `del_flag`，SQL 预检/清理前先查 `information_schema`。客户入口 `clientDashboard/orders`（待审）与商务入口 `business/customer-order`（可直接确认、合同后补）的 API/路由/权限/动作必须独立；Android 权限为 `business:customer-order:mobile`，Boss 代下单调用真实 `/system/business/customer-orders`，`recentOrders` 仅表示出入库历史，附件仅支持链接。订单确认后幂等创建同租户唯一活动 `ORDER_ROOT`（只读结构根），每个服务项目创建一个直接挂根的 `biz_entrustment_instance` DRAFT 子单；子单继承订单/客户/合同，仅是商务承接节点，不是部门作业单或 Go task。子单确认后按 `WORK_ORDER_CREATE:{domainCode}` 触发目标部门作业单；商务域只保存请求快照、`externalRef` 和状态摘要，不复制部门表、不另建审批/task-service 任务。合同仅经 `customer_order_id→sys_base_wt.ht_id→sys_contract` 追溯，不新增 `contract_id`、快照或关联表；无合同也可创建，`sys_base_wt` 只需 `(tenant_id,ht_id)` 索引。直接确认、审核、补合同统一经 `transitionInternal→createOrderRoot`，同事务建根和子单。仅已发布且版本不可变的模板可物化/提交；草稿可缺填，提交校验 required，失败回 DRAFT；`ext_schema_json` 为 `{schemaVersion,fields}`，切模板清空差异值，`date_range/datetime_range/time_range` 值均为 `[start,end]`。服务项目必须是 hh-system 按 ID 复验并固化快照的已发布 `ft_stock_price_cate`；删除订单须级联清理 `biz_customer_order_service_line`。角色仅精确归一化 `ht_商务/ht_商务部→BUSINESS`、`ht_操作部→OPERATION`，UI 按后端 `viewMode/domainCode/capabilities` 渲染。权威文档：`S:/note/xl-ht/商务委托单/08-客户订单子委托动态表单与任务闭环/`。 <!-- created=2026-08-03, last=2026-08-04 -->
§
xl-ht HT↔Go tenant 映射/上线约定：映射由系统管理员维护且严格一对一，每个 HT tenant 和 Go tenant 都只能绑定一次；新增 HT tenant 时新增独立绑定，普通用户不可切换，数据库连接不变。当前 HT 测试环境绑定既有 Go 测试 tenant 86；正式上线只把映射切到同一 Go 系统/PostgreSQL 中另一个既有正式 tenant，不新建数据库或 Go tenant，也不迁移测试 tenant 的历史审批、任务或流程数据。 <!-- created=2026-08-04, last=2026-08-04 -->